# Claim Firewall

Block:

```text
integration point selected without evidence
test plan without acceptance criteria mapping
off-limits file omitted
Fit Pack result treated as complete truth
sealed plan containing mutable signoff
production code written in PLAN mode
```
