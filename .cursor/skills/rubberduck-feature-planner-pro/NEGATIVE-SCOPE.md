# Negative Scope

Every plan must say what not to add.

Examples:

```text
no new dependency
no parallel config system
no public API change
no runtime path outside feature scope
no off-limits file modifications
```

Each negative scope row needs evidence or rationale.
