# Doppelganger Gate

For every new symbol, run cheap duplicate-intent prefilter. Run deep check only for threshold hits.

Similarity >= 0.75 requires:

```text
reuse
justify divergence
or explicit override
```
