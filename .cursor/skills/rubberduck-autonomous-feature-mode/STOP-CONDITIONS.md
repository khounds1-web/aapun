# Stop Conditions

Halt if:

```text
repo/base commit mismatch
envelope missing
off-limits violation
max_iterations reached
max_files_touched exceeded
new dependency not allowed
public API/schema change not allowed
Build final validation fails
```
