# Plan-Build Orchestration

Autonomous mode passes the Planner's sealed plan into Builder only after verifying:

```text
sealed_plan exists
plan hash computed
AUTO_AUTHORIZATION.json created
autonomy envelope permits build
```
