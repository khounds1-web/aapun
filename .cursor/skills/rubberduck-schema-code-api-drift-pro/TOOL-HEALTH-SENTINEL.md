# Tool Health Sentinel

Record:

```text
RubberDuck codebase status
semantic status
commit / instance_id
layer inventory coverage
unsupported file types
parser degradation
truncated outputs
analysis ID collisions
source-only fallbacks
```

If a tool fails, report:

```text
tool
attempted query
failure
blocks or slows
workaround
claim impact
```
