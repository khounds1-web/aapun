# Tool Health Sentinel

Record:

```text
RubberDuck codebase intelligence status
semantic intelligence status
commit / instance_id
loaded analyses
tool-call count
load truncation
cache eviction
fuzzy symbol matching
cross-analysis tool limitations
graph dimensions unavailable
local grep/source fallback used or not
unsupported languages/surfaces
```

If `analyze_code` compares only within one analysis, say so and compose pair comparison manually.

If a tool returns `limited`, do not use it as proof without source/graph corroboration.
